{
  "name": "es6_modules",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib",
    "watch:es6": "babel src -w -d lib",
    "start-server": "browser-sync start --server --files '*.html,lib/*.js' & npm run watch:es6",
    "babel-node": "babel-node",
    "uglifyjs": "uglifyjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "jquery": "^3.2.1"
  }
}
